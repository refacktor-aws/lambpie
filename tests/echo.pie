class Request:
    message: str
    number: int

class Response:
    status: str
    echo: str
    doubled: int

def handle(event: Request) -> Response:
    return Response("ok", event.message, event.number + event.number)
